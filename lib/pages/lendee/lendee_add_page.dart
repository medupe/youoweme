import 'package:flutter/material.dart';

class LendeeAddPage extends StatelessWidget {
  const LendeeAddPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
